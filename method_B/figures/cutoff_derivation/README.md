# Cut-off derivation figure

Run from this directory:

```bash
python fig_cutoff_derivation.py
```

The script reads:

```text
data/plot_data_best_loMNase.csv
data/plot_data_best_DNase.csv
data/plot_data_best_ATAC.csv
```

It writes:

- `fig_cutoff_derivation.png`
- `fig_cutoff_derivation.pdf`
- `fig_cutoff_derivation_thresholds.csv`

The copied script was changed from the local absolute-path version to relative paths so it can run after upload to GitHub.

